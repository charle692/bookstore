<div class="row">
	<div class="small-12 small-centered columns">
		<h1 class="text-center">Books for Sale</h1>	
	</div>	

	<% @categories.each do |label, books| %>
		<div class="row">
			<%= render partial: "slider_section", locals: {label: label, books: books} %>
		</div>
	<% end %>
</div>